# IntakeService — clarifies ambiguous topics before dispatching to minds

class IntakeService:
    async def clarify(self, raw_topic: str) -> dict:
        raise NotImplementedError("IntakeService not yet implemented")
