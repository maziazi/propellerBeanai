# MindRunner — dispatches topic to all 6 minds in parallel, collects results

class MindRunner:
    async def run_all(self, topic: str, context: dict = None) -> dict:
        raise NotImplementedError("MindRunner not yet implemented")
